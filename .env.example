# Plantilla para variables de entorno.
# Copiar este archivo como '.env' (con el punto inicial) y rellenar la clave.
# El archivo .env nunca debe subirse a un repositorio público.

GEMINI_API_KEY=tu_clave_de_gemini_aqui
